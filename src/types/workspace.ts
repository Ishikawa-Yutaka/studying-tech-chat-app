export interface Channel {
  id: number;
  description: string;
  name: string;
}

export interface DirectMessage {
  id: number;
  name: string;
}

export interface MyProfile {
  name: string;
  email: string;
}
